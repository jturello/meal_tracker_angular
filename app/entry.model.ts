export class Entry {
  constructor(public name: string, public description: string, public calories: string) {
    this.name = name;
    this.description = description;
    this.calories = calories;
  }
}
